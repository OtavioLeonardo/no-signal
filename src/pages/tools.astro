---
import Base from '@/layouts/Base.astro';
import Section from '@/components/Section.astro';

// 1. 数据结构改为分组模式
const toolGroups = [
  {
    category: "Sim Racing",
    icon: "🏁",
    color: "text-red-500",
    list: [
      {
        name: "LMU PitWall",
        description: "Le Mans Ultimate 必备策略数据查询与计算工具。",
        url: "https://otavioleonardo.github.io/lmu-pitwall/",
        icon: "🏎️",
        tag: "Web App"
      },
      {
        name: "Motec Setup Guide",
        description: "基于数据分析的车辆调校速查表。",
        url: "#",
        icon: "📈",
        tag: "PDF"
      }
    ]
  },
  {
    category: "Development",
    icon: "💻",
    color: "text-blue-500",
    list: [
      {
        name: "Sudoku Solver",
        description: "回溯算法演示，快速求解数独谜题。",
        url: "#",
        icon: "🧩",
        tag: "Algorithm"
      },
      {
        name: "Clock App",
        description: "使用 Swift 开发的极简时钟应用。",
        url: "#",
        icon: "⌚",
        tag: "iOS"
      }
    ]
  },
  {
    category: "Life & Learning",
    icon: "☕",
    color: "text-amber-500",
    list: [
      {
        name: "My German Notes",
        description: "A1-B1 德语语法笔记与复习卡片。",
        url: "#",
        icon: "🇩🇪",
        tag: "Notion"
      }
    ]
  }
];
---

<Base head={{ title: 'Workspace', description: 'Curated tools and projects' }}>
  <Section>
    <div class="py-12 md:py-16 max-w-5xl mx-auto">
      
      {/* 头部：更简洁，减少留白 */}
      <div class="mb-12 border-b border-gray-100 dark:border-white/10 pb-8">
        <h1 class="text-3xl md:text-4xl font-bold tracking-tight text-gray-900 dark:text-white mb-3">
          Workspace
        </h1>
        <p class="text-base text-gray-500 dark:text-gray-400">
          A collection of tools, small projects, and digital toys.
        </p>
      </div>

      <div class="space-y-12">
        {toolGroups.map((group) => (
          <section class="animate-enter-content">
            
            {/* 分类标题 */}
            <div class="flex items-center gap-3 mb-5 px-1">
              <span class={`text-xl ${group.color}`}>{group.icon}</span>
              <h2 class="text-lg font-bold text-gray-900 dark:text-white tracking-tight">
                {group.category}
              </h2>
            </div>

            {/* 紧凑网格布局: 手机1列，电脑2列 */}
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
              {group.list.map((tool) => (
                <a 
                  href={tool.url} 
                  target={tool.url.startsWith('http') ? "_blank" : "_self"}
                  class="group relative flex items-start gap-4 p-4 rounded-xl bg-white dark:bg-[#151516] border border-black/5 dark:border-white/10 transition-all duration-200 hover:bg-gray-50 dark:hover:bg-white/5 hover:border-black/10 dark:hover:border-white/20"
                >
                  {/* Icon: 缩小尺寸，更精致 */}
                  <div class="shrink-0 flex items-center justify-center w-10 h-10 rounded-lg bg-gray-100 dark:bg-white/5 text-xl group-hover:scale-110 transition-transform duration-200">
                    {tool.icon}
                  </div>

                  {/* Content: 紧凑排列 */}
                  <div class="flex-1 min-w-0">
                    <div class="flex items-center justify-between mb-0.5">
                      <h3 class="font-semibold text-gray-900 dark:text-gray-100 truncate pr-2 group-hover:text-blue-600 dark:group-hover:text-blue-400 transition-colors">
                        {tool.name}
                      </h3>
                      {/* Tag: 右上角的小标签 */}
                      {tool.tag && (
                        <span class="text-[10px] font-medium px-1.5 py-0.5 rounded-md bg-gray-100 dark:bg-white/10 text-gray-500 dark:text-gray-400 border border-gray-200 dark:border-white/5">
                          {tool.tag}
                        </span>
                      )}
                    </div>
                    <p class="text-sm text-gray-500 dark:text-gray-400 line-clamp-2 leading-relaxed">
                      {tool.description}
                    </p>
                  </div>

                  {/* External Link Arrow (Opactiy hover effect) */}
                  <div class="absolute top-4 right-4 text-gray-400 opacity-0 -translate-x-1 group-hover:opacity-100 group-hover:translate-x-0 transition-all">
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M7 17l9.2-9.2M17 17V7H7"/></svg>
                  </div>
                </a>
              ))}
            </div>
          </section>
        ))}
      </div>

    </div>
  </Section>
</Base>